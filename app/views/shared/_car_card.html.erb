<% @cars.each do |car| %>
  <%= link_to car_path(car) do %>
    <div class="card">
      <%= cl_image_tag car.photo.key, class: "card-image" %>
      <div class="card-text">
        <span class="date"><%= car.model %></span>
        <h2><%= car.brand %></h2>
        <p><%= car.description %></p>
      </div>
      <div class="card-stats">
        <div class="stat">
          <div class="value"><%= car.price %></div>
          <div class="type"><i class="material-icons me-2">euro</i></div>
        </div>
        <div class="stat stat-border">
          <div class="value"><%= car.capacity %></div>
          <div class="type"><i class="material-icons me-2">people</i></div>
        </div>
        <div class="stat">
          <div class="value"><%= car.occasion %></div>
        </div>
      </div>
    </div>
  <% end %>
<% end %>

<%# <div class="card-container" data-controller="card" data-action="mouseover->card#hoverDelete mouseout->card#hoverDelete"> %>
    <%# <div class="car-image" style="background-image: url(<%= car.image %>
      <%# <span><%= link_to "+", car_path(car), method: :delete, data: { confirm: "Are you sure?", "card-target"=>"delete"}, class: "card-remove-link"%>
    <%# </div> %>
<%# </div> %>
